{
  "name": "projet-finder",
  "private": true,
  "scripts": {
    "start": "npm --prefix finder/api start",
    "dev": "npm --prefix finder/api run dev",
    "check": "npm --prefix finder/api run check",
    "test": "npm --prefix finder/api test",
    "db:seed": "npm --prefix finder/api run db:seed"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "bcrypt": "^6.0.0"
  },
  "devDependencies": {
    "prisma": "^6.19.3"
  }
}
